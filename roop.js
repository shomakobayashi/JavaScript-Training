"user strict";

const rate = 1.1;

let = command = Number(prompt('menu 1, 2, 3 or 0 to exit'));

// console.log(`menu ${command} @rpcessed.`);

while (command !== 0) {
  console.log(`menu ${command} processed.`);
  command = Number(prompt('menu 1, 2, 3 or 0 to exit'));
}